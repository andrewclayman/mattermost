# DEPRECATED

This world has been removed because the underlying platform has fewer than 20 services in its default deployment. The world-factory targets only highly complex platforms with 20+ services.
